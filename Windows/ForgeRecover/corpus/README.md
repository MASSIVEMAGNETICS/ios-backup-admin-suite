# Recovery corpus workspace

Do not commit raw personal iOS backups here.

Use this folder only for generated/sanitized fixtures and manifest examples. Real authorized corpus cases should live in controlled local or encrypted storage and use `*.forge-recovery.json` manifests with acquisition hashes/provenance retained outside Git.
